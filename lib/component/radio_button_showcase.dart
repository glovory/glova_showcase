import 'package:flutter/material.dart';

class RadioButtonShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Radio Button Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Radio Button Showcase'),
      ),
    );
  }
}