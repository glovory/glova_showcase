import 'package:flutter/material.dart';

class TabsShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tabs Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Tabs Showcase'),
      ),
    );
  }
}