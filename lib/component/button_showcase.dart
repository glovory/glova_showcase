import 'package:flutter/material.dart';

class ButtonShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Button Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Button Showcase'),
      ),
    );
  }
}