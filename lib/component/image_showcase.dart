import 'package:flutter/material.dart';

class ImageShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Image Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Image Showcase'),
      ),
    );
  }
}