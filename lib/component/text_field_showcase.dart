import 'package:flutter/material.dart';

class TextFieldShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Text Field Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Text Field Showcase'),
      ),
    );
  }
}
