import 'package:flutter/material.dart';

class AvatarShowCase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Avatar Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Avatar Showcase'),
      ),
    );
  }
}