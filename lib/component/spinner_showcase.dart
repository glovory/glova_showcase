import 'package:flutter/material.dart';

class SpinnerShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Spinner Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Spinner Showcase'),
      ),
    );
  }
}