import 'package:flutter/material.dart';

class CardShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Card Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Card Showcase'),
      ),
    );
  }
}
