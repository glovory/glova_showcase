import 'package:flutter/material.dart';

class CheckBoxShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Check Box Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Check Box Showcase'),
      ),
    );
  }
}