import 'package:flutter/material.dart';

class IconButtonShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Icon Button Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Icon Button Showcase'),
      ),
    );
  }
}