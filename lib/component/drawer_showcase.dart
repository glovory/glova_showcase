import 'package:flutter/material.dart';

class DrawerShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Drawer Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Drawer Showcase'),
      ),
    );
  }
}