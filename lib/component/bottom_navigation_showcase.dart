import 'package:flutter/material.dart';

class BottomNavigationShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bottom Navigation Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Bottom Navigation Showcase'),
      ),
    );
  }
}