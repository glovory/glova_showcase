import 'package:flutter/material.dart';

class SelectShowcase extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Select Showcase'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Select Showcase'),
      ),
    );
  }
}
